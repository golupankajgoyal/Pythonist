from math import inf


def max_substring(str, k):
    win_start = 0
    count = 0
    char_set = set()

    for win_end, char in enumerate(str):
        char_set.add(char)


arr = [3, 4, 1, 1, 6]
S=8
print(min_sum(arr, S))































